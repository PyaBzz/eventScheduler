import { The } from '../refs'
import { assert } from 'chai'

describe(The.EntryService, () => {
    describe('get', () => {
        it('works', () => {
            assert.equal(1, 1);
        })
    })
});